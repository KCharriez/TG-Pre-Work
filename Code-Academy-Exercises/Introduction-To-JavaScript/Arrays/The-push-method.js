const chores = ['wash dishes', 'do laundry', 'take out trash'];

chores.push('finish JS coding', 'wrap presents');

console.log(chores);

//How we can modify existing information inside an array. One of these is throught the use of the .push() method which can add new elements to the array.
