console.log('First message!');
setTimeout(() => {
   console.log('This message will always run last...');
}, 0);
console.log('Second message!');

//Understanding Java as an asynchronous language, meaning that parts of the code can run while others are being loaded in, allowing a document to be read while parts of it are still being rendered.
